import { ChevronLeft, ChevronRight } from "lucide-react";
import { useState } from "react";
import { useAuditoria } from "../../hooks/useConfiguracoes";
import { Section } from "./shared";
export function AuditoriaSection(){const[page,setPage]=useState(1);const q=useAuditoria(page);return <Section title="Log de auditoria" description="Histórico das alterações administrativas realizadas pelo backend.">{q.isLoading?<p className="text-sm text-text-secondary">Carregando...</p>:<><div className="overflow-auto rounded border border-border"><table className="w-full min-w-[760px] text-left text-xs"><thead className="bg-surface2 text-text-secondary"><tr>{['Data','Usuário','Ação','Recurso','IP'].map(h=><th key={h} className="px-3 py-2">{h}</th>)}</tr></thead><tbody>{q.data?.items.map(i=><tr key={i.id} className="border-t border-border"><td className="px-3 py-2">{new Date(i.created_at).toLocaleString('pt-BR')}</td><td className="px-3 py-2">{i.usuario_nome??'Sistema'}</td><td className="px-3 py-2">{i.acao}</td><td className="px-3 py-2">{i.recurso}</td><td className="px-3 py-2 text-text-secondary">{i.ip_address??'—'}</td></tr>)}</tbody></table>{!q.data?.items.length&&<p className="p-5 text-center text-sm text-text-secondary">Nenhuma alteração registrada.</p>}</div><div className="mt-3 flex items-center justify-end gap-2 text-xs"><button disabled={page<=1} onClick={()=>setPage(p=>p-1)} className="rounded border border-border p-2 disabled:opacity-30"><ChevronLeft size={14}/></button><span>Página {page} de {Math.max(q.data?.total_pages??1,1)}</span><button disabled={page>=(q.data?.total_pages??1)} onClick={()=>setPage(p=>p+1)} className="rounded border border-border p-2 disabled:opacity-30"><ChevronRight size={14}/></button></div></>}</Section>}
